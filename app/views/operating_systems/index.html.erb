<%= render "shared/page_title", title: "Operating System Index" %>

<div class="row">
  <div class="col-md-6 col-md-offset-3 center">
    <% @operating_systems.each do |operating_system| %>
        <h4><strong><%= link_to "#" + operating_system.id.to_s + " - " + operating_system.os, operating_system_path(operating_system) %></strong></h4>
    <% end %>
  </div>
</div>